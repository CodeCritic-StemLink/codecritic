# Copy this file to .env and fill in the real values.
# .env is ignored by git and must never be committed. It holds passwords.

# The PostgreSQL connection string from Neon.
# Ask Osini for the real one, or get it from the Neon dashboard.
# Looks like: postgresql://user:password@ep-something.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=
